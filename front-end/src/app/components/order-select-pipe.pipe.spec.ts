import { OrderSelectPipePipe } from './order-select-pipe.pipe';

describe('OrderSelectPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSelectPipePipe();
    expect(pipe).toBeTruthy();
  });
});
